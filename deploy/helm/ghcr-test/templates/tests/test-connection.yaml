apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ghcr-test.fullname" . }}-test-connection"
  labels:
    {{- include "ghcr-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ghcr-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
